# Nexus Sidebar — Clean PR

This branch is intentionally simplified so you can review and merge a **single clean directory**:

## ✅ Use this folder
- `Nexus-V1/nexus-sidebar/`

## Why
Previous PRs had too many overlapping paths and conflicts.
This cleanup keeps one canonical snapshot for direct merge.

## Quick install
1. Open extensions page (`chrome://extensions`, `edge://extensions`, or `opera://extensions`).
2. Enable Developer Mode.
3. Load unpacked from `Nexus-V1/nexus-sidebar/`.

## Notes
- Includes welcome onboarding flow + language choice (English + Simplified Chinese).
- Includes background worker, sidebar UI, popup, and docs.
